# Backlog

Open work with no single line to mark.
A defect that has one wrong line gets a `BUG:` comment there instead, and is not copied here -- `rg -n 'BUG:|TODO:'` lists those.

One entry per line: priority, area, one sentence.
An entry needing a repro, a link, or what was already tried carries an indented block under it.

## Bugs

## Tech debt

## Explorations
