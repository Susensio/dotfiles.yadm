---
description: Markdown prose conventions for .md files.
paths:
  - "**/*.md"
---

# Markdown

Prose breaks by sentence, one per line, never hard wrapped to a column.
Frontmatter, fenced code and tables stay verbatim.
Where a repo's tracked markdown already holds to another convention, it wins.
